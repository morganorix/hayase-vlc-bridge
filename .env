# ============================================================
# hayase-vlc-bridge - configuration
# Copy this file to ".env" (same directory as the script) and edit values.
# ============================================================

# ------------------------------------------------------------
# Logging
# ------------------------------------------------------------
# 0 = no file logging
# 1 = simple logs
# 2 = detailed logs (includes masked config dump)
LOG_VERBOSITY=0

# Log file location
# You can freely change both the directory and filename.
# Tips:
#   - Prefer an existing directory
#   - Use an absolute path or $HOME/...
LOG_DIR="$HOME/<YOUR_LOCAL_DIRECTORY_LOGS>/"
LOG_FILE="$LOG_DIR/hayase-vlc-bridge.log"

# ------------------------------------------------------------
# Remote VLC
# ------------------------------------------------------------
# Remote VLC endpoint (host or host:port).
# Examples:
#   REMOTE_HOST="xxxxx.local"
#   REMOTE_HOST="192.168.x.xx"
#   REMOTE_HOST="192.168.x.xx:8080"
REMOTE_HOST="<URL_REMOTE>"

# WebSocket URL used by websocat.
# If your endpoint requires a path, set it explicitly:
#   WS_URL="ws://$REMOTE_HOST/your/path"
WS_URL="ws://$REMOTE_HOST"

# ------------------------------------------------------------
# Stream rewriting
# ------------------------------------------------------------
# Local IP reachable by the remote device (Apple TV / Android TV / etc.).
# The script rewrites:
#   http://localhost:PORT/...  -> http://STREAM_HOST:PORT/...
#   http://127.0.0.1:PORT/...  -> http://STREAM_HOST:PORT/...
#
# Example:
#   STREAM_HOST="192.168.x.xx"
STREAM_HOST="<YOUR_LOCAL_IP>"

# ------------------------------------------------------------
# Local VLC fallback
# ------------------------------------------------------------
# Local VLC command (string form). Used only for fallback when remote playback fails.
# Examples:
#   Flatpak: VLC_LOCAL_CMD="flatpak run org.videolan.VLC --one-instance"
#   Native : VLC_LOCAL_CMD="vlc --one-instance"
#   AppImage: VLC_LOCAL_CMD="$HOME/Applications/VLC.AppImage --one-instance"
#
# NOTE:
#   This string is parsed into arguments (split into an argv array).
#   Avoid complex shell constructs (pipes, redirections, command substitutions).
VLC_LOCAL_CMD="<VLC_INSTANCE>"
